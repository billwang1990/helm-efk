apiVersion: v1
kind: Service
metadata:
  name: {{ include "efk-yq.fullname" . }}
  labels:
    {{- include "efk-yq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "efk-yq.selectorLabels" . | nindent 4 }}
